# Notebooks

## Usage

Installing packages, from repo root (`ticket-forge`), run:

```sh
uv add scikit-learn --package research-notebooks
```

### VSCode
Select open notebook, set kernel as `.venv/bin/python`, and then you are done!


### Web Format

Simply go to repo root and run `uv run jupyter lab`