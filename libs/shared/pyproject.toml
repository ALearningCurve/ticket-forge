[project]
name = "shared"
version = "0.1.0"
description = "Shared utilities for logging, configuration, and common helpers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
